spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    defer-datasource-initialization: true
  datasource:
    url:
    username:
    password:
  h2:
    console:
      enabled: true

jwt:
  issuer:
  secret:
